# Legaceats

This app is for storing legacy recipes with the family.

Dependencies:

    - Node
    - Yarn

To run this project: `yarn start`

To test this project: `yarn test`

To build: `yarn build`
